<h1> Welcome <%= @user.username %> to your awesome dashboard </h1>
<a href="/users/index"> Click here to browse and search></a>
<h3> Skillswapr currently has <%= User.count %> members</h3></br>
<h3> Your skills are : <%= @user.teachable_skills.map &:name %></h3></br>
<h3> And you want to learn: <%= @user.learnable_skills.map &:name %></h3></br>

</br>

<h3> People looking for your skills </h3>
<% @user.find_my_students.each do |student| %>
  <p> Student: <a href="/users/<%= student.id %>"> <%= student.username %> </a>
  who wants to learn 
  <%= ((student.learnable_skills.map &:name) & (@user.teachable_skills.map &:name))%>
  </p>
  </br>
<% end %>

<h3> People who can help you learn</h3>
<% @user.find_my_teachers.each do |teacher| %>
  <p> Teacher: <a href="/users/<%= teacher.id %>"> <%= teacher.username %> </a>
  who can help you with
  <%= ((teacher.teachable_skills.map &:name) & (@user.learnable_skills.map &:name))%>
  </p>
  </br>
<% end %>